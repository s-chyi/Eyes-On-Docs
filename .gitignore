# .venv
# frontend/node_modules
.env
logs
test.json
last_crawl_time.txt
localtest*
target_config.json
# Nick-only smoke test config (single AML topic, empty webhook) — never commit
target_config-nick-smoke.json
target_config-nick*.json
# include-link resolver cache — regenerated at runtime, not shared
include_link_cache.json
# static
# .azure/
__pycache__/

# Next.js
web/.next/
web/out/
web/build/
web/node_modules/
web/.env*.local

# Next.js build output
web/.next
web/out

# Dependencies
web/node_modules
web/.pnp
web/.pnp.js

# Testing
web/coverage

# Debug
web/npm-debug.log*
web/yarn-debug.log*
web/yarn-error.log*

# Local env files
web/.env*.local

# Vercel
web/.vercel

# TypeScript
web/*.tsbuildinfo
web/next-env.d.ts

# Claude credentials (DO NOT commit)
.claude/vm_credentials.env
# VM state snapshot pulled from production (contains secrets)
.claude/vm-snapshot/
# Claude Code per-machine local settings (permissions/env/MCP list)
.claude/settings.local.json
# Claude Code runtime state (session state / plans / checkpoints)
.claude/investigation-state.json
.claude/investigation-state/
.claude/plans/
.claude/checkpoints/
.claude/cases/
.claude/hook-debug.log
.claude/hook-debug.log.*
.claude/scheduled_tasks.json
# ACA deployment runtime state (secrets, logs, execution names — Nick-only)
.claude/aca-deploy-state/
# Ad-hoc helper scripts (Nick-only bootstrap)
.claude/scripts/

# include_link_resolver runtime cache (auto-generated at repo root by include_link_resolver.py)
include_link_cache.json
